{
  "id": "group-agg",
  "title": {
    "tr": "Rapor tablosu",
    "en": "A report table"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "`agg` içinde `yeni_ad=(\"sütun\", \"hesap\")` biçimini kullan.",
      "en": "Inside `agg`, use the form `new_name=(\"column\", \"calculation\")`."
    },
    {
      "tr": "Üç sütun istiyoruz: `people=(\"name\", \"count\")`, `average=(\"score\", \"mean\")`, `highest=(\"score\", \"max\")`.",
      "en": "We want three columns: `people=(\"name\", \"count\")`, `average=(\"score\", \"mean\")`, `highest=(\"score\", \"max\")`."
    },
    {
      "tr": "Sonuç bir tablo; `round(1)` ile yuvarla, tek hücreyi `report.loc[\"Izmir\", \"people\"]` ile al.",
      "en": "The result is a table; round it with `round(1)` and take a single cell with `report.loc[\"Izmir\", \"people\"]`."
    },
    {
      "tr": "Çözüm:\n\n```python\nreport = data.groupby(\"city\").agg(\n    people=(\"name\", \"count\"),\n    average=(\"score\", \"mean\"),\n    highest=(\"score\", \"max\"),\n).round(1)\n\nprint(report)\nprint(report.loc[\"Izmir\", \"people\"])\n```",
      "en": "The solution:\n\n```python\nreport = data.groupby(\"city\").agg(\n    people=(\"name\", \"count\"),\n    average=(\"score\", \"mean\"),\n    highest=(\"score\", \"max\"),\n).round(1)\n\nprint(report)\nprint(report.loc[\"Izmir\", \"people\"])\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "        people  average  highest\ncity                            \nAnkara       3     87.0       91\nBursa        2     69.0       70\nIzmir        3     71.3       76\n3"
    }
  ]
}
