{
  "id": "two-keys",
  "title": {
    "tr": "İki anahtarla grupla",
    "en": "Group by two keys"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "İki anahtar için liste veriyorsun: `data.groupby([\"city\", \"grade\"])`.",
      "en": "For two keys you pass a list: `data.groupby([\"city\", \"grade\"])`."
    },
    {
      "tr": "Sonucun index'i iki seviyeli oluyor. `reset_index()` seviyeleri sütuna çeviriyor.",
      "en": "The result gets a two-level index. `reset_index()` turns the levels into columns."
    },
    {
      "tr": "Düzleştirilmiş tablonun boyutunu `.shape` veriyor.",
      "en": "`.shape` gives the shape of the flattened table."
    },
    {
      "tr": "Çözüm:\n\n```python\nresult = data.groupby([\"city\", \"grade\"])[\"score\"].mean().round(1)\n\nprint(result)\nprint(result.reset_index().shape)\n```",
      "en": "The solution:\n\n```python\nresult = data.groupby([\"city\", \"grade\"])[\"score\"].mean().round(1)\n\nprint(result)\nprint(result.reset_index().shape)\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "city    grade\nAnkara  A        86.5\n        B        88.0\nBursa   B        70.0\n        C        68.0\nIzmir   A        76.0\n        B        74.0\n        C        64.0\nName: score, dtype: float64\n(7, 3)"
    }
  ]
}
