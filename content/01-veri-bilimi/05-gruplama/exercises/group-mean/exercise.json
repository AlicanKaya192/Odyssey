{
  "id": "group-mean",
  "title": {
    "tr": "Şehre göre ortalama",
    "en": "Average by city"
  },
  "difficulty": 1,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "`data.groupby(\"city\")[\"score\"].mean()` — soldan sağa okunuyor: grupla, sütunu al, hesapla.",
      "en": "`data.groupby(\"city\")[\"score\"].mean()` — read left to right: group, take the column, compute."
    },
    {
      "tr": "Grup başına satır sayısını `data.groupby(\"city\").size()` veriyor.",
      "en": "`data.groupby(\"city\").size()` gives the number of rows per group."
    },
    {
      "tr": "Sonuç bir seri; üzerine `idxmax()` uygulayınca en yüksek ortalamalı grubun adı geliyor.",
      "en": "The result is a Series; calling `idxmax()` on it gives the name of the group with the highest average."
    },
    {
      "tr": "Çözüm:\n\n```python\naverages = data.groupby(\"city\")[\"score\"].mean()\n\nprint(averages.round(2))\nprint(data.groupby(\"city\").size())\nprint(averages.idxmax())\n```",
      "en": "The solution:\n\n```python\naverages = data.groupby(\"city\")[\"score\"].mean()\n\nprint(averages.round(2))\nprint(data.groupby(\"city\").size())\nprint(averages.idxmax())\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "city\nAnkara    87.00\nBursa     69.00\nIzmir     71.33\nName: score, dtype: float64\ncity\nAnkara    3\nBursa     2\nIzmir     3\ndtype: int64\nAnkara"
    }
  ]
}
