{
  "id": "inspect-data",
  "title": {
    "tr": "Önce bak",
    "en": "Look first"
  },
  "difficulty": 1,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "`shape` bir demet veriyor: `(satır, sütun)`.",
      "en": "`shape` gives a tuple: `(rows, columns)`."
    },
    {
      "tr": "Sütun adlarını liste hâlinde görmek için `list(raw.columns)`. Boşluklara dikkat et.",
      "en": "Use `list(raw.columns)` to see the column names as a list. Watch the spaces."
    },
    {
      "tr": "Tabloda toplam kaç eksik olduğunu `raw.isna().sum().sum()` veriyor; `int()` ile sayıya çevir.",
      "en": "`raw.isna().sum().sum()` gives the total number of gaps; wrap it in `int()`."
    },
    {
      "tr": "Çözüm:\n\n```python\nprint(raw.shape)\nprint(list(raw.columns))\nprint(raw.dtypes.astype(str).tolist())\nprint(int(raw.isna().sum().sum()))\nprint(int(raw.duplicated().sum()))\n```",
      "en": "The solution:\n\n```python\nprint(raw.shape)\nprint(list(raw.columns))\nprint(raw.dtypes.astype(str).tolist())\nprint(int(raw.isna().sum().sum()))\nprint(int(raw.duplicated().sum()))\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "(7, 3)\n[' Name ', 'city', 'score']\n['str', 'str', 'str']\n0\n0"
    }
  ]
}
