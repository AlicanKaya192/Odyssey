{
  "id": "clean-names",
  "title": {
    "tr": "Sütun adlarını düzelt",
    "en": "Fix the column names"
  },
  "difficulty": 1,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "Önce kopyala: `data = raw.copy()`. Ham veriyi bozmamak iyi bir alışkanlık.",
      "en": "Copy first: `data = raw.copy()`. Not damaging the raw data is a good habit."
    },
    {
      "tr": "Sütun adları da bir seri gibi davranıyor: `data.columns.str.strip()`.",
      "en": "The column names behave like a Series: `data.columns.str.strip()`."
    },
    {
      "tr": "Zincirleyebilirsin: `data.columns.str.strip().str.lower()`. Sonucu `data.columns` değişkenine ata.",
      "en": "You can chain them: `data.columns.str.strip().str.lower()`. Assign the result back to `data.columns`."
    },
    {
      "tr": "Çözüm:\n\n```python\ndata = raw.copy()\ndata.columns = data.columns.str.strip().str.lower()\n\nprint(list(data.columns))\nprint(data[\"name\"].tolist())\n```",
      "en": "The solution:\n\n```python\ndata = raw.copy()\ndata.columns = data.columns.str.strip().str.lower()\n\nprint(list(data.columns))\nprint(data[\"name\"].tolist())\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "['name', 'city', 'score']\n[' Ada ', 'kerem', 'MINA', 'Ada ', 'Deniz', 'efe ', 'Sila']"
    }
  ]
}
