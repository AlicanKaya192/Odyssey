{
  "id": "build-table",
  "title": {
    "tr": "İlk tablon",
    "en": "Your first table"
  },
  "difficulty": 1,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "Sözlükten tablo kuruyorsun: anahtarlar sütun adı, değerler sütunun içeriği.",
      "en": "You build a table from a dictionary: the keys are column names and the values the contents."
    },
    {
      "tr": "`shape` bir demet veriyor: `(satır, sütun)`. Parantezsiz, çünkü bir özellik.",
      "en": "`shape` gives a tuple: `(rows, columns)`. No parentheses — it is an attribute."
    },
    {
      "tr": "Sütun adlarını liste hâlinde yazdırmak için `list(data.columns)` kullan. İlk iki satır için `head(2)`.",
      "en": "Use `list(data.columns)` to print the column names as a list. For the first two rows, `head(2)`."
    },
    {
      "tr": "Çözüm:\n\n```python\nprint(data.shape)\nprint(list(data.columns))\nprint(data.head(2))\nprint(round(data[\"score\"].mean(), 1))\n```",
      "en": "The solution:\n\n```python\nprint(data.shape)\nprint(list(data.columns))\nprint(data.head(2))\nprint(round(data[\"score\"].mean(), 1))\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "(5, 3)\n['name', 'city', 'score']\n    name    city  score\n0    Ada  Ankara     82\n1  Kerem   Izmir     74\n80.6"
    }
  ]
}
