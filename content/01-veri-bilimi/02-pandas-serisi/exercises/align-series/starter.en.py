import pandas as pd

first = pd.Series([40, 55, 70], index=["Ada", "Kerem", "Mina"])
second = pd.Series([30, 25, 60], index=["Mina", "Ada", "Kerem"])
extra = pd.Series([10], index=["Efe"])

# Add the two Series together into a Series called total.


# Print total and the name with the highest total.


# Put total + extra into with_extra and print how many NaN it contains.
