{
  "id": "series-filter",
  "title": {
    "tr": "Pahalıları ayır",
    "en": "Separate the expensive ones"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "Koşullu seçim NumPy'daki gibi: `prices[prices >= 100]`.",
      "en": "Selecting by condition works as in NumPy: `prices[prices >= 100]`."
    },
    {
      "tr": "Sonuç yine bir seri — etiketler de geliyor. Kaç tane olduğunu `.size` veriyor.",
      "en": "The result is still a Series — the labels come along. `.size` gives how many."
    },
    {
      "tr": "Ortalamayı yazdırırken `round(..., 1)` kullan.",
      "en": "Use `round(..., 1)` when printing the mean."
    },
    {
      "tr": "Çözüm:\n\n```python\nexpensive = prices[prices >= 100]\n\nprint(expensive)\nprint(expensive.size)\nprint(round(expensive.mean(), 1))\n```",
      "en": "The solution:\n\n```python\nexpensive = prices[prices >= 100]\n\nprint(expensive)\nprint(expensive.size)\nprint(round(expensive.mean(), 1))\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "kalem    120\ncanta    240\nkitap    175\ndtype: int64\n3\n178.3"
    }
  ]
}
