import pandas as pd

prices = pd.Series(
    [120, 85, 240, 60, 175],
    index=["kalem", "defter", "canta", "silgi", "kitap"],
)

# Collect the ones priced 100 or above into a Series called expensive.


# Print expensive, how many there are and the average price.
