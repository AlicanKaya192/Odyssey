import pandas as pd

temps = pd.Series(
    [21.0, None, 24.0, None, 27.0, 30.0],
    index=["mon", "tue", "wed", "thu", "fri", "sat"],
)

# How many readings are missing? Keep it in missing.


# Keep the mean in a variable called average.


# Produce a Series called filled with the gaps filled by that mean.


# Print missing, count and size side by side, the mean and filled as a list.
