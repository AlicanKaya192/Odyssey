{
  "id": "count-cities",
  "title": {
    "tr": "Hangisinden kaç tane?",
    "en": "How many of each?"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "`value_counts()` her değerden kaç tane olduğunu çoktan aza sıralayarak veriyor.",
      "en": "`value_counts()` gives how many of each value there are, most first."
    },
    {
      "tr": "Kaç **farklı** şehir olduğunu `nunique()` veriyor.",
      "en": "`nunique()` gives how many **distinct** cities there are."
    },
    {
      "tr": "`value_counts()` bir seri döndürüyor; onun `idxmax()` değeri en çok tekrar eden şehri veriyor.",
      "en": "`value_counts()` returns a Series; its `idxmax()` gives the most frequent city."
    },
    {
      "tr": "Çözüm:\n\n```python\ncounts = cities.value_counts()\n\nprint(counts)\nprint(cities.nunique())\nprint(counts.idxmax())\n```",
      "en": "The solution:\n\n```python\ncounts = cities.value_counts()\n\nprint(counts)\nprint(cities.nunique())\nprint(counts.idxmax())\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "Ankara    3\nIzmir     2\nBursa     1\nName: count, dtype: int64\n3\nAnkara"
    }
  ]
}
