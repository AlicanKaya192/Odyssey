{
  "id": "first-array",
  "title": {
    "tr": "İlk dizin",
    "en": "Your first array"
  },
  "difficulty": 1,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "Diziyi listeden kuruyorsun: `np.array([12, 7, 25, 3, 18])`.",
      "en": "You build the array from a list: `np.array([12, 7, 25, 3, 18])`."
    },
    {
      "tr": "`shape` ve `dtype` birer **özellik**, parantez yok: `values.shape`, `values.dtype`.",
      "en": "`shape` and `dtype` are **attributes**, no parentheses: `values.shape`, `values.dtype`."
    },
    {
      "tr": "`sum()` ve `mean()` ise **metot**, parantez var: `values.sum()`.",
      "en": "`sum()` and `mean()` are **methods**, with parentheses: `values.sum()`."
    },
    {
      "tr": "Çözüm:\n\n```python\nimport numpy as np\n\nvalues = np.array([12, 7, 25, 3, 18])\n\nprint(values.shape)\nprint(values.dtype)\nprint(values.sum())\nprint(values.mean())\n```",
      "en": "The solution:\n\n```python\nimport numpy as np\n\nvalues = np.array([12, 7, 25, 3, 18])\n\nprint(values.shape)\nprint(values.dtype)\nprint(values.sum())\nprint(values.mean())\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import numpy as np` satırı yok.",
        "en": "There is no `import numpy as np` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "(5,)\nint64\n65\n13.0"
    }
  ]
}
