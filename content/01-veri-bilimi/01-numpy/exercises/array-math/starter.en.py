import numpy as np

prices = np.array([100, 250, 80, 400])
counts = np.array([3, 1, 5, 2])

# Compute each product's revenue into an array called totals.


# Add 20% tax to the revenues, into an array called with_tax.


# Print totals, the rounded with_tax and the total revenue.
