import numpy as np

scores = np.array([80.0, np.nan, 90.0, 70.0, np.nan, 60.0])

# How many values are missing? Keep it in a variable called missing.


# Compute the average without counting the missing ones, into average.


# Take a copy of scores (called filled) and fill the gaps with the average.


# Print missing, average, filled and the mean of filled, in that order.
