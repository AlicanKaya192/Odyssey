{
  "id": "label-select",
  "title": {
    "tr": "Etikete göre seç",
    "en": "Select by label"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 15,
  "hints": [
    {
      "tr": "`set_index(\"name\")` adları satır etiketi yapıyor.",
      "en": "`set_index(\"name\")` makes the names the row labels."
    },
    {
      "tr": "`loc[satır, sütun]` tek hücre veriyor: `by_name.loc[\"Mina\", \"score\"]`.",
      "en": "`loc[row, column]` gives a single cell: `by_name.loc[\"Mina\", \"score\"]`."
    },
    {
      "tr": "`loc` dilimlerinde **bitiş dâhil**: `loc[\"Ada\":\"Mina\", \"score\"]` üç satır veriyor.",
      "en": "In `loc` slices the **end is included**: `loc[\"Ada\":\"Mina\", \"score\"]` gives three rows."
    },
    {
      "tr": "Çözüm:\n\n```python\nby_name = data.set_index(\"name\")\n\nprint(by_name.loc[\"Mina\", \"score\"])\nprint(by_name.loc[\"Ada\":\"Mina\", \"score\"])\nprint(by_name.loc[\"Kerem\", \"city\"])\nprint(len(by_name.loc[\"Ada\":\"Mina\"]))\n```",
      "en": "The solution:\n\n```python\nby_name = data.set_index(\"name\")\n\nprint(by_name.loc[\"Mina\", \"score\"])\nprint(by_name.loc[\"Ada\":\"Mina\", \"score\"])\nprint(by_name.loc[\"Kerem\", \"city\"])\nprint(len(by_name.loc[\"Ada\":\"Mina\"]))\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "91\nname\nAda      82\nKerem    74\nMina     91\nName: score, dtype: int64\nIzmir\n3"
    }
  ]
}
