import pandas as pd

data = pd.DataFrame({
    "team": ["A", "A", "A", "A", "B", "B", "B", "B", "C", "C"],
    "minutes": [31, 28, 35, 30, 27, 33, 29, 240, 30, 32],
})

# Find the outliers with the IQR rule and print them as a list.


# Print the mean and the median of all the data side by side.


# Print the count and mean by team, rounded to one decimal.
