{
  "id": "first-look",
  "title": {
    "tr": "İlk bakış",
    "en": "The first look"
  },
  "difficulty": 1,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 30,
  "hints": [
    {
      "tr": "`data.shape` satır ve sütun sayısını bir demet olarak veriyor.",
      "en": "`data.shape` gives the row and column counts as a tuple."
    },
    {
      "tr": "Toplam boş sayısı için `data.isna().sum().sum()` — iki kez toplama gerekiyor.",
      "en": "For the total number of blanks, `data.isna().sum().sum()` — it needs summing twice."
    },
    {
      "tr": "Tipleri okunur yazdırmak için `data.dtypes.astype(str).tolist()`.",
      "en": "To print the types readably, `data.dtypes.astype(str).tolist()`."
    },
    {
      "tr": "Çözüm:\n\n```python\nprint(data.shape)\nprint(data.dtypes.astype(str).tolist())\nprint(data.isna().sum().sum())\nprint(data[\"city\"].nunique())\n```",
      "en": "The solution:\n\n```python\nprint(data.shape)\nprint(data.dtypes.astype(str).tolist())\nprint(data.isna().sum().sum())\nprint(data[\"city\"].nunique())\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "Import",
      "hint": {
        "tr": "`import pandas as pd` satırı yok.",
        "en": "There is no `import pandas as pd` line."
      }
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "(10, 4)\n['str', 'int64', 'int64', 'int64']\n0\n3"
    }
  ]
}
