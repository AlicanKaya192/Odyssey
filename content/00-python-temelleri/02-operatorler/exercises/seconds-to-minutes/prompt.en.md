The variable `total_seconds` holds **200 seconds**. Work out how many minutes and seconds that is.

- Store the number of whole minutes in `minutes`.
- Store the leftover seconds in `seconds`.
- Print the result in this format:

```
3 min 20 sec
```

> Do not use `/` — it gives a decimal, and you would end up with something like `3.33 min`.
