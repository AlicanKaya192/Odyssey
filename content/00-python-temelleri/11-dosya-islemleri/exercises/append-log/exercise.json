{
  "id": "append-log",
  "title": {
    "tr": "Sona eklemek",
    "en": "Appending to the end"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 10,
  "hints": [
    {
      "tr": "İlk blokta `\"w\"` doğru: dosyayı sıfırdan kuruyorsun. Sonraki bloklarda `\"a\"` kullan.",
      "en": "`\"w\"` is right in the first block: you are creating the file from scratch. Use `\"a\"` in the blocks after it."
    },
    {
      "tr": "`\"a\"` kipi dosyanın sonundan devam ediyor; eski içeriğe dokunmuyor. `\"w\"` ise açar açmaz siliyor.",
      "en": "The `\"a\"` mode carries on from the end of the file and leaves the old contents alone. `\"w\"` deletes them the moment it opens."
    },
    {
      "tr": "Her `write` çağrısının sonuna `\\n` koymayı unutma, yoksa üç satır tek satıra biner ve liste tek elemanlı çıkar.",
      "en": "Do not forget the `\\n` at the end of every `write`, or the three lines end up as one and the list has a single element."
    },
    {
      "tr": "Çözüm:\n\n```python\nwith open(\"log.txt\", \"w\", encoding=\"utf-8\") as file:\n    file.write(\"start\\n\")\n\nwith open(\"log.txt\", \"a\", encoding=\"utf-8\") as file:\n    file.write(\"step one\\n\")\n\nwith open(\"log.txt\", \"a\", encoding=\"utf-8\") as file:\n    file.write(\"step two\\n\")\n\nwith open(\"log.txt\", encoding=\"utf-8\") as file:\n    entries = file.read().splitlines()\n\nprint(len(entries))\nprint(entries)\n```",
      "en": "The solution:\n\n```python\nwith open(\"log.txt\", \"w\", encoding=\"utf-8\") as file:\n    file.write(\"start\\n\")\n\nwith open(\"log.txt\", \"a\", encoding=\"utf-8\") as file:\n    file.write(\"step one\\n\")\n\nwith open(\"log.txt\", \"a\", encoding=\"utf-8\") as file:\n    file.write(\"step two\\n\")\n\nwith open(\"log.txt\", encoding=\"utf-8\") as file:\n    entries = file.read().splitlines()\n\nprint(len(entries))\nprint(entries)\n```"
    }
  ],
  "checks": [
    {
      "type": "variable",
      "name": "entries",
      "equals": [
        "start",
        "step one",
        "step two"
      ]
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "3\n['start', 'step one', 'step two']"
    }
  ]
}
