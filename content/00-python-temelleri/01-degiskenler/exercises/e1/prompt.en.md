Define two variables:

- Create a variable called `takim` and put `Beşiktaş` in it.
- Create a variable called `yil` and put the number `1903` in it.

Then print the following as a single line:

```
Beşiktaş 1903
```

There must be **exactly one space** between the team name and the year.

> Hint: if you use a comma inside `print()`, Python adds the space for you.
