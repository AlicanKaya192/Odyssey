# Write the parse_line function.
# If the line has no "=", raise ValueError: "bad line: " + line
# If it does, split it in two at the first "=" and return both parts.


lines = ["name=Ada", "broken", "city=London"]
settings = {}

for line in lines:
    # Try parse_line(line).
    # If a ValueError comes up, print the error's message.
    # If not, put the key and the value into settings.
    pass


# Print the settings dictionary.
