# The Task class: title and done (defaulting to False),
# a finish method and a __str__ method ("[x] title" or "[ ] title").


# The Board class: an empty tasks list (annotated list[Task]),
# an add method (returns the total count),
# a pending method (unfinished titles, list[str]).


# Build a Board and add the tasks "write", "test" and "ship".
# Finish the second one.


# Print the number of tasks, the text form of the second task
# and the pending titles, in that order.
