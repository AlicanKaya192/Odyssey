{
  "id": "sum-with-loop",
  "title": {
    "tr": "Döngüyle toplam",
    "en": "Sum with a loop"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 10,
  "hints": [
    {
      "tr": "Toplamı biriktirecek bir değişkene ihtiyacın var. Döngüden **önce** onu sıfırla, döngünün **içinde** üzerine ekle.",
      "en": "You need a variable to accumulate the total. Set it to zero **before** the loop, and add to it **inside** the loop."
    },
    {
      "tr": "Listenin elemanları üzerinde gezmek için `for number in numbers:` yazarsın. Döngünün gövdesi girintili olmalı. Her adımda `total = total + number` (ya da kısaca `total += number`) yaparsın.",
      "en": "To walk over the list write `for number in numbers:`. The loop body must be indented. On each step do `total = total + number` (or the shorter `total += number`)."
    },
    {
      "tr": "Çözüm şöyle:\n\n```python\nnumbers = [1, 2, 3, 4, 5]\n\ntotal = 0\nfor number in numbers:\n    total += number\n\nprint(total)\n```",
      "en": "Here is the solution:\n\n```python\nnumbers = [1, 2, 3, 4, 5]\n\ntotal = 0\nfor number in numbers:\n    total += number\n\nprint(total)\n```"
    }
  ],
  "checks": [
    {
      "type": "ast_require",
      "node": "For",
      "hint": {
        "tr": "Bu görevi bir `for` döngüsü ile çözmelisin.",
        "en": "You need to solve this with a `for` loop."
      }
    },
    {
      "type": "ast_forbid",
      "call": "sum",
      "hint": {
        "tr": "Hazır `sum()` fonksiyonunu kullanma, toplamı döngüyle kendin hesapla.",
        "en": "Do not use the built-in `sum()` function; compute the total yourself with a loop."
      }
    },
    {
      "type": "variable",
      "name": "total",
      "equals": 15
    },
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "15"
    }
  ]
}
