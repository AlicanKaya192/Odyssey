# Add type annotations to the repeat function.
# text is a string, count is a whole number, the return is a string.
def repeat(text, count):
    return text * count


# Print the results of repeat("ab", 3) and repeat("-", 5).
