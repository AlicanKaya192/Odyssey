# Define a function called calculate_average.
# One parameter: a list of numbers. Hand the average back with return.
# Find the total and the count with a loop; do not use sum().


scores = [10, 20, 30, 40]

# Call the function, put the result into average, and print it.
