# Define a function called total_price.
# Parameters: price, count, discount (discount defaults to 0)
# Hand the total back with return: price * count - discount


# Call the function twice:
# full    -> 3 items at a unit price of 50, no discount
# reduced -> the same purchase, with a discount of 20


# Print both, one under the other.
