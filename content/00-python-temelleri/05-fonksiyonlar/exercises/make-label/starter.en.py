# Define a function called make_label.
# Parameters: text, prefix (prefix defaults to "INFO")
# Return a string shaped like "LABEL: text".


# Call the function twice, into first and second.


# Print both, one under the other.
