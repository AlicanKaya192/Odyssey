There are twelve products in the table. Bring back the ones whose name
**starts with M**, in alphabetical order; the `name` column only.

```
name      
----------
Microphone
Monitor   
Mouse     
```

Exact equality is no use here — you do not know the whole name, only how
it **starts**.

`%` means zero or more characters.
