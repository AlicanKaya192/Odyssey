{
  "id": "tedarikcisi-olmayanlar",
  "language": "tsql",
  "title": {
    "tr": "Tedarikçisi kayıtlı olmayanlar",
    "en": "No supplier recorded"
  },
  "difficulty": 2,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.sql",
  "solution": "solution.sql",
  "timeout_sec": 30,
  "hints": [
    {
      "tr": "`NULL` bir değer değil, değerin **yokluğu**. Bu yüzden `=` ile karşılaştırılamıyor.",
      "en": "`NULL` is not a value but the **absence** of one. That is why `=` cannot compare it."
    },
    {
      "tr": "Doğru yazım `sutun IS NULL`; tersi için `IS NOT NULL`.",
      "en": "The correct form is `column IS NULL`; the opposite is `IS NOT NULL`."
    },
    {
      "tr": "Sorgunun tamamı:\n\n```sql\nSELECT name, category\nFROM products\nWHERE supplier_code IS NULL\nORDER BY name;\n```",
      "en": "The whole query:\n\n```sql\nSELECT name, category\nFROM products\nWHERE supplier_code IS NULL\nORDER BY name;\n```"
    }
  ],
  "checks": [
    {
      "type": "columns",
      "expected": [
        "name",
        "category"
      ]
    },
    {
      "type": "rows",
      "ordered": true,
      "expected": [
        [
          "Headset",
          "Accessory"
        ],
        [
          "Office Suite",
          "Software"
        ],
        [
          "Projector",
          "Display"
        ]
      ]
    },
    {
      "type": "sql_require",
      "pattern": "IS NULL",
      "hint": {
        "tr": "`= NULL` yazmak hiçbir satır döndürmüyor — bilinmeyen bir değerle yapılan karşılaştırmanın sonucu da bilinmiyor. Doğru yazım `IS NULL`.",
        "en": "Writing `= NULL` returns no rows — a comparison with an unknown value is itself unknown. The correct form is `IS NULL`."
      }
    }
  ]
}
