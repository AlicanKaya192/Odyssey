{
  "id": "hepsi-bir-arada",
  "language": "tsql",
  "title": {
    "tr": "Hepsi bir arada",
    "en": "All of it together"
  },
  "difficulty": 3,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.sql",
  "solution": "solution.sql",
  "timeout_sec": 30,
  "hints": [
    {
      "tr": "Üç koşul da `AND` ile bağlanıyor ve hepsi aynı `WHERE` içinde duruyor.",
      "en": "All three conditions are joined with `AND` inside the same `WHERE`."
    },
    {
      "tr": "Hepsi `AND` olduğu için parantez gerekmiyor — `AND`/`OR` karışımı olsaydı gerekirdi.",
      "en": "Since they are all `AND`, no parentheses are needed — they would be if `AND` and `OR` were mixed."
    },
    {
      "tr": "Sorgunun tamamı:\n\n```sql\nSELECT name, category, price\nFROM products\nWHERE category IN ('Accessory', 'Display')\n  AND price BETWEEN 200 AND 2000\n  AND supplier_code IS NOT NULL\nORDER BY price DESC;\n```",
      "en": "The whole query:\n\n```sql\nSELECT name, category, price\nFROM products\nWHERE category IN ('Accessory', 'Display')\n  AND price BETWEEN 200 AND 2000\n  AND supplier_code IS NOT NULL\nORDER BY price DESC;\n```"
    }
  ],
  "checks": [
    {
      "type": "columns",
      "expected": [
        "name",
        "category",
        "price"
      ]
    },
    {
      "type": "rows",
      "ordered": true,
      "expected": [
        [
          "Microphone",
          "Accessory",
          1320.0
        ],
        [
          "Webcam",
          "Accessory",
          1150.0
        ],
        [
          "Keyboard",
          "Accessory",
          450.0
        ],
        [
          "Mouse",
          "Accessory",
          220.0
        ]
      ]
    },
    {
      "type": "sql_require",
      "pattern": "IS NOT NULL",
      "hint": {
        "tr": "\"Boş değil\" koşulu `<> NULL` ile yazılamıyor; `IS NOT NULL` gerekiyor.",
        "en": "\"Not empty\" cannot be written as `<> NULL`; it needs `IS NOT NULL`."
      }
    }
  ]
}
