{
  "id": "residual-plot",
  "title": {
    "tr": "Kalıntı grafiği",
    "en": "The residual plot"
  },
  "difficulty": 4,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 90,
  "hints": [
    {
      "tr": "Bu kez kalıntılar **eğitim** verisinde: `y_train - model.predict(X_train)`. Ölçüm değil teşhis yapıyorsun.",
      "en": "This time the residuals are on the **training** data: `y_train - model.predict(X_train)`. You are diagnosing, not measuring."
    },
    {
      "tr": "Eğitim satırlarının yaşına `df.loc[X_train.index, \"age\"]` ile ulaşıyorsun.",
      "en": "Reach the training rows' ages with `df.loc[X_train.index, \"age\"]`."
    },
    {
      "tr": "Korelasyon için `residuals.corr(ages)`. Sıfır çizgisi için `plt.axhline(0, color=\"red\")`.",
      "en": "For the correlation use `residuals.corr(ages)`. For the zero line, `plt.axhline(0, color=\"red\")`."
    },
    {
      "tr": "Çözüm:\n\n```python\nimport matplotlib.pyplot as plt\nimport pandas as pd\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.model_selection import train_test_split\n\ndf = pd.read_csv(\"homes.csv\")\nX = df[[\"area\"]]\ny = df[\"price\"]\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, random_state=42)\n\nmodel = LinearRegression()\nmodel.fit(X_train, y_train)\n\nresiduals = y_train - model.predict(X_train)\nages = df.loc[X_train.index, \"age\"]\n\nplt.scatter(ages, residuals)\nplt.axhline(0, color=\"red\")\nplt.xlabel(\"age\")\nplt.ylabel(\"residual\")\nplt.title(\"Residuals against age\")\nplt.savefig(\"chart.png\")\n\nprint(round(residuals.corr(ages), 3))\n```",
      "en": "The solution:\n\n```python\nimport matplotlib.pyplot as plt\nimport pandas as pd\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.model_selection import train_test_split\n\ndf = pd.read_csv(\"homes.csv\")\nX = df[[\"area\"]]\ny = df[\"price\"]\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, random_state=42)\n\nmodel = LinearRegression()\nmodel.fit(X_train, y_train)\n\nresiduals = y_train - model.predict(X_train)\nages = df.loc[X_train.index, \"age\"]\n\nplt.scatter(ages, residuals)\nplt.axhline(0, color=\"red\")\nplt.xlabel(\"age\")\nplt.ylabel(\"residual\")\nplt.title(\"Residuals against age\")\nplt.savefig(\"chart.png\")\n\nprint(round(residuals.corr(ages), 3))\n```"
    }
  ],
  "checks": [
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "-0.937"
    },
    {
      "type": "artifact",
      "name": "chart.png",
      "min_bytes": 1000
    }
  ]
}
