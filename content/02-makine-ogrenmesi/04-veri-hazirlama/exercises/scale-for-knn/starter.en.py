# Write the imports, read the file, drop the rows with gaps.


# Take the three numeric columns into X and price into y. Split.


# Fit a StandardScaler on training and apply it to both.


# Train KNN (n_neighbors=5) on raw and on scaled data.
# Print the two MAEs side by side (two decimals).


# Train linear regression on raw and scaled too, print side by side.


# Are linear regression's two results the same? Print "same"/"different".
