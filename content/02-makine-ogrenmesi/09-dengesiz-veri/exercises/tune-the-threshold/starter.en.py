# Write the imports, prepare, split, scale the data and train the model.


# Get the positive class's probability with predict_proba.


# Try the thresholds 0.5, 0.4, 0.3, 0.2, 0.1 and 0.05 in turn.
# Print one line per threshold: the threshold, precision, recall, F1 and
# the number caught. Keep the F1 values together with their thresholds.


# Print the threshold giving the highest F1 and that F1 value.
