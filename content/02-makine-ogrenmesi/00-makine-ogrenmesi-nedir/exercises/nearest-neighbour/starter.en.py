points = [
    ((2.0, 3.0), "A"),
    ((3.0, 3.5), "A"),
    ((8.0, 7.0), "B"),
    ((7.5, 8.0), "B"),
    ((2.5, 2.0), "A"),
]
new_point = (7.0, 7.5)

# Unpack the new point's coordinates.


# Build a (distance, label) pair for every point.


# Sort the distances from small to large.


# Print the sorted distances to two decimals as a list.


# Print the label of the nearest point.


# Print the labels of the three nearest points as a list.
