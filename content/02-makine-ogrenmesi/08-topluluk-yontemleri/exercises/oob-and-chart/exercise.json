{
  "id": "oob-and-chart",
  "title": {
    "tr": "Bedava doğrulama",
    "en": "Free validation"
  },
  "difficulty": 4,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 200,
  "hints": [
    {
      "tr": "`oob_score=True` verilince `model.oob_score_` doluyor; `bootstrap=True` şart (varsayılan).",
      "en": "With `oob_score=True` the `model.oob_score_` gets filled; `bootstrap=True` is required (the default)."
    },
    {
      "tr": "Üç listeyi biriktiriyorsun: ağaç sayıları, OOB skorları, test skorları.",
      "en": "You collect three lists: the tree counts, the OOB scores and the test scores."
    },
    {
      "tr": "Grafikte iki eğri var; `plt.legend()` etiketleri gösteriyor.",
      "en": "The chart has two curves; `plt.legend()` shows the labels."
    },
    {
      "tr": "Çözüm:\n\n```python\nimport matplotlib.pyplot as plt\nimport pandas as pd\nfrom sklearn.model_selection import train_test_split\n\ndf = pd.read_csv(\"customers.csv\")\nX = df[[\"age\", \"income\", \"visits\"]]\ny = df[\"churn\"]\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, random_state=42, stratify=y)\n\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.metrics import accuracy_score\n\ncounts = [10, 25, 50, 100, 200]\noob_scores = []\ntest_scores = []\n\nfor count in counts:\n    model = RandomForestClassifier(n_estimators=count, oob_score=True,\n                                   random_state=42)\n    model.fit(X_train, y_train)\n    oob = float(model.oob_score_)\n    test = accuracy_score(y_test, model.predict(X_test))\n    oob_scores.append(oob)\n    test_scores.append(test)\n    print(count, round(oob, 3), round(test, 3))\n\nplt.plot(counts, oob_scores, label=\"oob\", marker=\"o\")\nplt.plot(counts, test_scores, label=\"test\", marker=\"o\")\nplt.xlabel(\"trees\")\nplt.ylabel(\"accuracy\")\nplt.title(\"Out-of-bag against test\")\nplt.legend()\nplt.savefig(\"chart.png\")\n\nprint(round(abs(oob_scores[-1] - test_scores[-1]), 3))\n```",
      "en": "The solution:\n\n```python\nimport matplotlib.pyplot as plt\nimport pandas as pd\nfrom sklearn.model_selection import train_test_split\n\ndf = pd.read_csv(\"customers.csv\")\nX = df[[\"age\", \"income\", \"visits\"]]\ny = df[\"churn\"]\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, random_state=42, stratify=y)\n\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.metrics import accuracy_score\n\ncounts = [10, 25, 50, 100, 200]\noob_scores = []\ntest_scores = []\n\nfor count in counts:\n    model = RandomForestClassifier(n_estimators=count, oob_score=True,\n                                   random_state=42)\n    model.fit(X_train, y_train)\n    oob = float(model.oob_score_)\n    test = accuracy_score(y_test, model.predict(X_test))\n    oob_scores.append(oob)\n    test_scores.append(test)\n    print(count, round(oob, 3), round(test, 3))\n\nplt.plot(counts, oob_scores, label=\"oob\", marker=\"o\")\nplt.plot(counts, test_scores, label=\"test\", marker=\"o\")\nplt.xlabel(\"trees\")\nplt.ylabel(\"accuracy\")\nplt.title(\"Out-of-bag against test\")\nplt.legend()\nplt.savefig(\"chart.png\")\n\nprint(round(abs(oob_scores[-1] - test_scores[-1]), 3))\n```"
    }
  ],
  "checks": [
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "10 0.873 0.86\n25 0.873 0.9\n50 0.88 0.88\n100 0.893 0.9\n200 0.887 0.9\n0.013"
    },
    {
      "type": "artifact",
      "name": "chart.png",
      "min_bytes": 1000
    }
  ]
}
