# Write the imports: RandomForestClassifier, GradientBoostingClassifier,
# DecisionTreeClassifier, StratifiedKFold, cross_val_score, accuracy_score.
# Prepare and split the data. NO scaling.


# Print the baseline (the most frequent class, three decimals).


# Take three models in turn: tree, forest, boosting.
# Print one line each: the name, the test accuracy, the CV mean and the
# CV spread. Cross validate on the TRAINING data only.


# Print the test winner and the CV winner side by side.


# Print "different" if they differ, "same" if they agree.
