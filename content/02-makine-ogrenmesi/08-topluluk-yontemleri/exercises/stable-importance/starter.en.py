# Write the imports, prepare and split the data.


# Train two models: tree (max_depth=2) and forest (200 trees).


# Print one line each: the name and the three importance values.


# How many columns got exactly zero importance? Tree first, then forest.
