# Write the imports, prepare and split the data.


# Six rounds with seed from 0 to 5:
#   sample 90% of the training data with that seed, match the labels,
#   train a tree (max_depth=3) and a forest (200 trees),
#   keep both test accuracies and the tree's root threshold.


# Print the three lists on separate lines.


# Print the two ranges side by side: the tree's and the forest's.
