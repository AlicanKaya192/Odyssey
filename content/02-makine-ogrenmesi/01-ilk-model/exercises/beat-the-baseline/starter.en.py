# Write the imports, read the file, prepare X and y.


# Split the data: a quarter for testing, random_state=42.


# The baseline: the mean price of the training data. Print it (two decimals).


# The baseline's mean absolute error on the test set.


# Train the model and compute its mean absolute error.


# Print the two errors side by side: baseline first, then model.


# Print "better" if the model beat the baseline, otherwise "worse".
