{
  "id": "two-features",
  "title": {
    "tr": "İkinci özellik",
    "en": "A second feature"
  },
  "difficulty": 3,
  "prompt": {
    "tr": "prompt.tr.md",
    "en": "prompt.en.md"
  },
  "starter": "starter.{lang}.py",
  "solution": "solution.py",
  "timeout_sec": 60,
  "hints": [
    {
      "tr": "Tek değişen `X`: `df[[\"area\", \"age\"]]`. Kodun geri kalanı aynı kalıyor.",
      "en": "Only `X` changes: `df[[\"area\", \"age\"]]`. The rest of the code stays the same."
    },
    {
      "tr": "İki katsayı var: `model.coef_[0]` ve `model.coef_[1]`. Listeyi doğrudan yazdırma, iki sayıyı ayrı ayrı yuvarla.",
      "en": "There are two coefficients: `model.coef_[0]` and `model.coef_[1]`. Do not print the list itself; round the two numbers separately."
    },
    {
      "tr": "Son satırda iki özellikli modelin hatasını tek özellikli modelinkiyle (18.5) karşılaştırıyorsun.",
      "en": "On the last line you compare the two-feature model's error with the single-feature one (18.5)."
    },
    {
      "tr": "Çözüm:\n\n```python\nimport pandas as pd\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.metrics import mean_absolute_error\nfrom sklearn.model_selection import train_test_split\n\ndf = pd.read_csv(\"homes.csv\")\nX = df[[\"area\", \"age\"]]\ny = df[\"price\"]\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, random_state=42)\n\nmodel = LinearRegression()\nmodel.fit(X_train, y_train)\nmodel_mae = mean_absolute_error(y_test, model.predict(X_test))\n\nprint(round(model.coef_[0], 2), round(model.coef_[1], 2))\nprint(round(model_mae, 2))\nprint(\"better\" if model_mae < 18.5 else \"worse\")\n```",
      "en": "The solution:\n\n```python\nimport pandas as pd\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.metrics import mean_absolute_error\nfrom sklearn.model_selection import train_test_split\n\ndf = pd.read_csv(\"homes.csv\")\nX = df[[\"area\", \"age\"]]\ny = df[\"price\"]\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, random_state=42)\n\nmodel = LinearRegression()\nmodel.fit(X_train, y_train)\nmodel_mae = mean_absolute_error(y_test, model.predict(X_test))\n\nprint(round(model.coef_[0], 2), round(model.coef_[1], 2))\nprint(round(model_mae, 2))\nprint(\"better\" if model_mae < 18.5 else \"worse\")\n```"
    }
  ],
  "checks": [
    {
      "type": "stdout",
      "match": "trimmed_lines",
      "expected": "2.77 -3.35\n7.13\nbetter"
    }
  ]
}
