# Import everything you need:
# pandas, train_test_split, LinearRegression, mean_absolute_error.


# Read the file.


# Take the area column as a table into X and price as a column into y.


# Split the data: a quarter for testing, random_state=42.


# Print the training and test counts side by side.


# Build the model and train it.


# Print the slope and the intercept side by side (two decimals).


# Print the mean absolute error on the test set (two decimals).
