points = [
    ((2.0, 1.0), "A"), ((1.5, 2.0), "A"), ((3.0, 1.5), "A"),
    ((7.0, 8.0), "B"), ((8.0, 7.5), "B"), ((6.5, 7.0), "B"),
    ((4.5, 4.0), "A"), ((5.0, 5.5), "B"),
]
new_point = (5.0, 4.5)

# Unpack the new point's coordinates.


# Build a (distance, label) pair for every point, rounding the distance
# to two decimals, and sort the list.


# Print the sorted distances as a list.


# For k = 1, 3, 5 in turn:
# take the labels of the nearest k neighbours, find the majority and
# print one line: k, the labels, the winner.
