# Write the imports and read the file.


# Take the three columns into X and churn into y.


# Split: a quarter for testing, random_state=42, stratify=y.


# Print the training and test counts side by side.


# The baseline: predict the most frequent class for every test record
# and print its accuracy (three decimals).


# Fit a StandardScaler on training and apply it to both.


# Train two KNNs (n_neighbors=5): raw and scaled.
# Print the two accuracies side by side.


# Print "worse" if the raw KNN is below the baseline, otherwise "better".
