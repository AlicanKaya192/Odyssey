# Write the imports: you also need StratifiedKFold and cross_val_score.
# Read the file, prepare X and y, split.


# PART ONE
# For depths 1, 2, 3, 5, 8 and None measure the training and test accuracy
# and print one line: depth, training, test. Write "none" for None.


# PART TWO
# Build a StratifiedKFold (5 folds, shuffle=True, random_state=42).
# Cross validate on the TRAINING data only for depths 1, 2, 3, 5, None;
# print depth, mean and spread for each.
# Keep the depth with the highest mean and print it at the end.
