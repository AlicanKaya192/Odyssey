# Write the imports: you also need export_text.
# Read the file, prepare X and y, split.


# Train a tree with max_depth=2 (random_state=42).


# Print the rules with export_text (do not forget the column names).


# Print the root split's feature and threshold side by side.


# Print the number of leaves and the depth side by side.
