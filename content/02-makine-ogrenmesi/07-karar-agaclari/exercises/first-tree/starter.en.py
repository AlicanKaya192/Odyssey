# Write the imports: pandas, train_test_split, DecisionTreeClassifier,
# KNeighborsClassifier, StandardScaler, accuracy_score.


# Read the file, prepare X and y, split (random_state=42, stratify=y).


# The baseline: the most frequent class.


# The decision tree: max_depth=3, random_state=42. Do NOT scale.


# KNN: k=25 on scaled data.


# Print the three accuracies side by side: baseline, tree, knn.


# Print "better" if the tree beats the baseline, otherwise "worse".


# Which model is better? Print "knn" or "tree".
