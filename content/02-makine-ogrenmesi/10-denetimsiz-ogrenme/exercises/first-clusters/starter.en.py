# Write the imports: pandas, StandardScaler, KMeans, silhouette_score
# and numpy. Read the data, take the four columns and scale them.


# Cluster with KMeans (n_clusters=4, random_state=42, n_init=10).


# Print the cluster sizes as a list.


# Make the labels a "cluster" column and print the profile table.


# Print the silhouette score.
