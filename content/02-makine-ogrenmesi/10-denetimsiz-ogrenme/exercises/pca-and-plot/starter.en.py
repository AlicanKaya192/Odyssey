# Write the imports: matplotlib.pyplot, PCA, KMeans, silhouette_score,
# adjusted_rand_score and numpy. Prepare and scale the data.


# Compute every component and print the cumulative explained variance.


# Refit with two components and transform.
# Print the two components' weights on separate lines.


# Cluster separately in the full space and in the PCA space.
# Print the two silhouettes side by side.


# The adjusted_rand_score between the two clusterings.


# Draw the PCA-space points as a scatter coloured by the full-space
# cluster label, and save as chart.png.
