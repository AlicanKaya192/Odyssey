# Write the imports, prepare and scale the data.


# Generate a standard normal noise array of the same shape as X_scaled
# (numpy.random.default_rng(0)).


# Try k from 2 to 5. Print one line per k: k, the real data's silhouette,
# the noise's silhouette. Keep the two silhouettes at k=4 somewhere.


# Print the cluster sizes found in the noise at k=4, as a sorted list.


# The ratio of the two silhouettes at k=4.
