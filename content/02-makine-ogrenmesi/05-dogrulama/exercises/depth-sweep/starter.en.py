# Write the imports: pandas, train_test_split, DecisionTreeRegressor,
# mean_absolute_error.


# Read the file, drop the rows with gaps, encode fuel and gearbox.


# Take every column but price into X and price into y. Split.


# Try the depths 1, 2, 3, 5, 8 and None in turn.
# Measure the training and test error for each and print one line:
# the depth, the training error, the test error. Print "none" for None.
