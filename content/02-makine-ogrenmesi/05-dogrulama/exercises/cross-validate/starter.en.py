# Write the imports: you also need KFold and cross_val_score.
# Prepare the data; DO NOT split it.


# Build a KFold: 5 folds, shuffle=True, random_state=42.


# Measure a linear regression with cross_val_score
# (scoring="neg_mean_absolute_error").


# Flip the sign, round to two decimals, print the list.


# Print the mean and the spread (standard deviation) side by side.


# Does 17.07 fall between the lowest and highest fold?
# Print "inside" or "outside".
