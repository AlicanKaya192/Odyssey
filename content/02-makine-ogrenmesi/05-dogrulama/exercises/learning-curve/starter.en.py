# Write the imports: you also need matplotlib.pyplot.
# Prepare and split the data (random_state=42).


# Sizes: 10, 20, 30, 45, 60, 79
# For each size train the model on the first that many training rows,
# measure the training error on that portion and the error on the test set,
# collect both in lists and print one line.


# Draw both curves on one chart, label them, add a legend.


# Label the axes, add a title, save as chart.png.


# Print "no" if the gap at the last size is below 1, otherwise "yes".
