# Write the imports, prepare and split the data.


# Take the two models in turn: "simple" (depth 1), "complex" (depth None).
# Measure the training and test error for each.
# If the gap exceeds 20 print "overfit"; otherwise if the training error
# exceeds 50 print "underfit"; if neither, "ok".
# Print one line: the name, the training error, the test error, the label.
