# Write the imports and read the file.


# Take the three columns into X and passed into y.


# Split: a quarter for testing, random_state=42, stratify=y.


# Print the training and test counts side by side.


# Build the model and train it (max_iter=1000).


# The baseline: predict the most frequent class for every test record.


# Print the baseline's and the model's accuracy side by side (3 decimals).


# Print "better" if the model beat the baseline, otherwise "worse".
