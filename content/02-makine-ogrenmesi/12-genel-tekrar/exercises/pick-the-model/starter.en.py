# Write the imports: StratifiedKFold, cross_val_score, LogisticRegression,
# KNeighborsClassifier, RandomForestClassifier, GradientBoostingClassifier,
# roc_auc_score, average_precision_score.
# Prepare and split the data (no followup_calls, stratify=y).


# Set up the StratifiedKFold.


# Take four models in turn: logreg, knn, forest, boosting.
# Print one line each: the name, the CV average precision, the CV spread,
# the test ROC AUC, the test average precision.
# Use scoring="average_precision" for the cross validation.


# Average precision's baseline (the positive rate).


# The CV winner.
