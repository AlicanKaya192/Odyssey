# Uygulamanın kendi bağımlılıkları.
# Alıştırmaların çalıştığı ortam ayrı: requirements-exercises.txt

PySide6>=6.8,<7        # arayüz + gömülü PDF görüntüleyici (QtPdf)
markdown>=3.5          # ders metinlerini HTML'e çevirmek için
pypdf>=4.0             # sadece tools/import_content.py kullanıyor

# Uygulama bunları **import etmiyor**; alıştırma kodunu çalıştıran
# denetleyici kullanıyor. Burada duruyorlar çünkü paketleyicinin onları
# .exe'nin içine koyabilmesi için kurulu olmaları gerekiyor.
numpy>=2.0             # Veri Bilimi alıştırmaları
pandas>=2.2            # Veri Bilimi alıştırmaları
matplotlib>=3.8        # Veri Bilimi alıştırmaları
