# Kullanıcının yazdığı alıştırma kodlarının çalıştığı sanal ortam.
# Uygulamanın kendi ortamından ayrıdır; buraya eklenen her paket
# alıştırmalarda import edilebilir hale gelir.

numpy>=1.26
pandas>=2.2
matplotlib>=3.8
scikit-learn>=1.4

# SQL alıştırmaları için: T-SQL, makinedeki MSSQL sunucusunda çalışıyor.
# Sürücünün kendisi (ODBC Driver for SQL Server) Microsoft'tan ayrıca
# kuruluyor; pyodbc yalnızca ona bağlanan katman.
pyodbc>=5.1
